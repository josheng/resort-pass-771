
<%= simple_form_for :search, url: search_listing_path, method: "GET", html: { class: 'form-inline d-flex justify-content-center' } do |f| %>
  <%= f.input :name, input_html: {value: "", style:"width: 500px;"} , label: false %>
  <%= f.submit "Search", class: "btn btn-primary" %>

<% end %>
